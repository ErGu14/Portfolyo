@inject HttpClient Http

<nav id="main-navbar" class="navbar navbar-expand-lg fixed-top py-3">
    <div class="container">
        <a class="navbar-brand text-white fw-bold fs-3" href="#">
            Eray<span style="color: var(--accent-primary);">G.</span>
        </a>
        <button class="navbar-toggler border-0" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" style="filter: invert(1) grayscale(100%) brightness(200%);">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
            <ul class="navbar-nav gap-4 align-items-center">
                <li class="nav-item">
                    <a class="nav-link text-white fw-semibold hover-accent" href="#about-section">Hakkımda</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link text-white fw-semibold hover-accent" href="#skills-section">Yetenekler</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link text-white fw-semibold hover-accent" href="#projects-section">Projeler</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link text-white fw-semibold hover-accent" href="#certificates-section">Sertifikalar</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link text-white fw-semibold hover-accent" href="#contact-section">İletişim</a>
                </li>
            </ul>
        </div>
    </div>
</nav>

<style>
    #main-navbar {
        transition: all 0.3s ease;
        background: transparent;
    }
    
    #main-navbar.scrolled {
        background: rgba(10, 25, 47, 0.9);
        backdrop-filter: blur(10px);
        -webkit-backdrop-filter: blur(10px);
        box-shadow: 0 10px 30px -10px rgba(2, 12, 27, 0.7);
        padding-top: 15px !important;
        padding-bottom: 15px !important;
    }

    .hover-accent {
        transition: color 0.3s ease;
    }
    .hover-accent:hover {
        color: var(--accent-primary) !important;
    }

    @@media (max-width: 991.98px) {
        .navbar-collapse {
            background-color: rgba(10, 25, 47, 0.98);
            backdrop-filter: blur(10px);
            -webkit-backdrop-filter: blur(10px);
            border-radius: 10px;
            padding: 1.5rem;
            margin-top: 1rem;
            border: 1px solid rgba(100, 255, 218, 0.1);
            box-shadow: 0 10px 30px -10px rgba(2, 12, 27, 0.7);
        }
        
        .navbar-nav {
            gap: 1rem !important;
        }
    }
</style>
